{
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 100,
    "startCommand": "python3 startup.py"
  },
  "envVars": {
    "DATABASE_URL": {
      "required": true
    },
    "SUPABASE_SSLMODE": {
      "required": false,
      "value": "require"
    }
  }
}


